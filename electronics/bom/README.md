# BOM

- `List_of_electronic_components_updated.xlsx`: current bill of materials (availability + qty + notes + links).

Rule: any change to the BOM must be paired with:
1) a commit message explaining why,
2) an issue link (e.g., "updates for M3"),
3) a short note in the README.
